# Fixtures are ACM Canonical JSON / golden YAML — formatters must never touch them
packages/conformance/fixtures/
# Generated artifacts are produced by the drift pipeline, never formatted by hand
packages/spec/generated/
node_modules/
pnpm-lock.yaml
apm_modules/
.specify/
.claude/
specs/
docs/
CONTEXT.md
LICENSE
llms.txt
# The Discovery Skill text layout is content: blocks are byte-compared across
# targets and generated/ is drift-pipeline territory (invariant #1)
packages/discovery-skill/
# Self-contained Nx testbed with its own formatter config; outside the pnpm workspace
examples/
