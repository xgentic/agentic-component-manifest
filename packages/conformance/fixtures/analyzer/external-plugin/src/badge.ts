/** A small inline status badge. */
export function StatusBadgeWidget(props: {
  /** Text shown inside the badge. */
  label: string;
  /** Visual tone of the badge. */
  tone: "info" | "warn" | "error";
}): void {
  void props;
}
