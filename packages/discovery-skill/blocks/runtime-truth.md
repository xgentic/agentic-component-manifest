## On version skew

If an option, envelope, or code behaves differently than this skill describes,
the installed CLI wins: run `acm capabilities --json` and follow it. It is
derived from the same command registry the parser runs on, so it cannot drift
from actual behavior — and it may be newer than this regenerated skill text.
