# Claude Code — nx-angular-testbed

@AGENTS.md

The `acm-discovery` skill is installed at `.claude/skills/acm-discovery/`. It should
activate automatically for any frontend UI task in this workspace — you do not need to
invoke it by name.
